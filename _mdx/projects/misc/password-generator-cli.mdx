---
title: Password Generator CLI
tags: misc, cli, security, password-generation, terminal
description: Build a command-line password generator with customizable length, character sets, and security options
expertise: easy
thumbId: GlavtG-umzE
---

Create a command-line tool that generates secure passwords with customizable options for length, character types, and special requirements.

<h1>Project Checklist</h1>
<ul>
  <li>Create CLI interface with argument parsing</li>
  <li>Implement password generation algorithm</li>
  <li>Add options for length, character sets (uppercase, lowercase, numbers, symbols)</li>
  <li>Include password strength validation</li>
  <li>Add option to copy password to clipboard</li>
  <li>Support multiple password generation</li>
</ul>
<h2>Bonus Project Checklist Items</h2>
<ul>
  <li>Add passphrase generation mode</li>
  <li>Implement password history (encrypted storage)</li>
  <li>Add password strength meter</li>
  <li>Create password pattern options (memorable, random)</li>
  <li>Add export to file functionality</li>
  <li>Implement password sharing (secure, time-limited)</li>
</ul>
<h2>Inspiration (Any companies/libraries similar)</h2>
<ul>
  <li>1Password CLI</li>
  <li>LastPass CLI</li>
  <li>pwgen</li>
</ul>
<h2>Hint/Code snippet to start</h2>
```python
import secrets
import string
import argparse

def generate_password(length=16, include_uppercase=True, include_lowercase=True, 
                     include_numbers=True, include_symbols=True):
    chars = ''
    if include_uppercase:
        chars += string.ascii_uppercase
    if include_lowercase:
        chars += string.ascii_lowercase
    if include_numbers:
        chars += string.digits
    if include_symbols:
        chars += string.punctuation
    
    return ''.join(secrets.choice(chars) for _ in range(length))

if __name__ == '__main__':
    parser = argparse.ArgumentParser()
    parser.add_argument('-l', '--length', type=int, default=16)
    args = parser.parse_args()
    print(generate_password(args.length))
```